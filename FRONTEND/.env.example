# Optional: Set this to your live Render backend URL (e.g. https://satquery-ai.onrender.com)
# When set, Vite directs all API calls and asset requests directly to Render.
# In local development without this variable, Vite proxies /api to http://127.0.0.1:8000.
VITE_API_BASE_URL=
